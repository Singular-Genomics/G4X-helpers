common: &common
  - -v
  - "2"

commands:
#   migrate:
#     - *common
#     - migrate
#     - "{data_dir}"

  resegment:
    - *common
    - resegment
    - "{data_dir}"
    - --cell-labels
    - "{data_dir}/masks/segmentation_mask.npz"
    - --labels-key
    - nuclei_exp
  
  redemux:
    - *common
    - redemux
    - "{data_dir}"
    - --manifest
    - "{data_dir}/transcript_manifest.csv"
